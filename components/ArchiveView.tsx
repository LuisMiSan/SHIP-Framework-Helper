import React from 'react';

// This component is now deprecated as its functionality has been merged
// into the WelcomeScreen component to create an integrated dashboard experience.
const ArchiveView: React.FC<any> = () => {
  return null;
};

export default ArchiveView;